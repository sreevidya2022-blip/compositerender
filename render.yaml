services:
  - type: web
    name: compositelab
    env: python
    plan: free
    pythonVersion: 3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GROQ_API_KEY
        scope: build_and_runtime
```

5. Click **"Commit changes"**

---

## 🎯 **OR SIMPLEST: Use .python-version file**

Create new file called `.python-version`:

**Content (exactly this):**
```
3.11.9
